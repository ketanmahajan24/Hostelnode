<!-- City Landing Page -->

<!DOCTYPE html>
<html lang="en">
<head>
  <!-- Google tag -->
  <script async src="https://www.googletagmanager.com/gtag/js?id=G-3J9E7QW77B"></script>
  <script>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date()); gtag('config', 'G-3J9E7QW77B');
  </script>

  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= cityData.meta.title %></title>
  <link rel="icon" type="image/png" href="/images/hostelnode-logo-cutout.png">

  <!-- SEO Meta -->
  <meta name="description" content="<%= cityData.meta.description %>">
  <meta name="keywords"    content="<%= cityData.meta.keywords %>">
  <meta name="robots"      content="index, follow">
  <link rel="canonical"    href="https://www.hostelnode.com/city/<%= cityData.slug %>">

  <!-- OG / Social -->
  <meta property="og:title"       content="<%= cityData.meta.title %>">
  <meta property="og:description" content="<%= cityData.meta.description %>">
  <meta property="og:image"       content="https://www.hostelnode.com/images/hostelnode-logo-cutout.png">
  <meta property="og:url"         content="https://www.hostelnode.com/city/<%= cityData.slug %>">
  <meta property="og:type"        content="website">
  <meta name="twitter:card"       content="summary_large_image">

  <!-- JSON-LD Schema for Google -->
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "WebPage",
    "name": "<%= cityData.meta.title %>",
    "description": "<%= cityData.meta.description %>",
    "url": "https://www.hostelnode.com/city/<%= cityData.slug %>",
    "breadcrumb": {
      "@type": "BreadcrumbList",
      "itemListElement": [
        { "@type": "ListItem", "position": 1, "name": "Home",       "item": "https://www.hostelnode.com" },
        { "@type": "ListItem", "position": 2, "name": "Find Hostel","item": "https://www.hostelnode.com/findHostels" },
        { "@type": "ListItem", "position": 3, "name": "PG in <%= cityData.name %>", "item": "https://www.hostelnode.com/city/<%= cityData.slug %>" }
      ]
    }
  }
  </script>

  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css" crossorigin="anonymous">
  <link href="https://fonts.googleapis.com/css2?family=Instrument+Sans:ital,wght@0,400..700;1,400..700&family=Inter:opsz,wght@14..32,300..700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/findhostel.css">
  <link rel="stylesheet" href="/css/style.css">  <!-- ADD THIS LINE -->

  <link rel="stylesheet" href="/css/navbar.css">  <!-- ADD THIS LINE -->
  <style>

    /* ── CITY PAGE SPECIFIC ── */
    .city-hero {
      background: #fff;
      border-bottom: 1px solid var(--border);
      padding: 48px clamp(16px,5vw,60px) 0;
      position: relative; overflow: hidden;
      text-align: center;
    }
    .city-hero::before {
      content: '';
      position: absolute; inset: 0;
      background-image: radial-gradient(var(--green-mid) 1.2px, transparent 1.2px);
      background-size: 28px 28px; opacity: 0.4; pointer-events: none;
    }
    .city-hero::after {
      content: '';
      position: absolute; top: 0; left: 50%; transform: translateX(-50%);
      width: 700px; height: 200px;
      background: radial-gradient(ellipse at 50% 0%, rgba(9,184,80,0.10) 0%, transparent 70%);
      pointer-events: none;
    }
    .city-hero-inner { position: relative; z-index: 1; max-width: 800px; margin: 0 auto; }
    .city-breadcrumb {
      display: flex; align-items: center; justify-content: center; gap: 6px;
      font-size: 0.8rem; color: var(--muted); margin-bottom: 16px; flex-wrap: wrap;
    }
    .city-breadcrumb a { color: var(--green); font-weight: 600; }
    .city-breadcrumb i { font-size: 0.6rem; opacity: 0.5; }
    .city-emoji { font-size: 3rem; margin-bottom: 10px; display: block; }
    .city-hero h1 {
      font-family: var(--font-d);
      font-size: clamp(1.8rem, 4vw, 3rem);
      font-weight: 700; color: var(--ink);
      line-height: 1.15; letter-spacing: -0.02em;
      margin-bottom: 12px;
    }
    .city-hero h1 span { color: var(--green); }
    .city-hero-sub {
      font-size: 1rem; color: var(--muted); max-width: 540px; margin: 0 auto 24px; line-height: 1.7;
    }
    .city-stats-row {
      display: flex; justify-content: center; gap: 28px; flex-wrap: wrap;
      padding: 20px 0; border-top: 1px solid var(--border); margin-top: 28px;
    }
    .city-stat { text-align: center; }
    .city-stat-num {
      font-family: var(--font-d); font-size: 1.5rem; font-weight: 700; color: var(--ink);
    }
    .city-stat-num span { color: var(--green); }
    .city-stat-label { font-size: 0.78rem; color: var(--muted); margin-top: 2px; }

    /* ── SEARCH BAR (inline on city page) ── */
    .city-search-wrap {
      max-width: 600px; margin: 0 auto 24px;
    }
    .city-search-form {
      display: flex; gap: 8px;
      background: #fff;
      border: 1.5px solid var(--border);
      border-radius: 50px;
      padding: 6px 6px 6px 18px;
      box-shadow: var(--shadow-md);
    }
    .city-search-form input {
      flex: 1; border: none; outline: none; background: transparent;
      font-family: var(--font-b); font-size: 0.9rem; color: var(--ink);
    }
    .city-search-form input::placeholder { color: var(--muted-light); }
    .city-search-form button {
      padding: 9px 22px; background: var(--green); color: #fff; border: none;
      border-radius: 50px; font-weight: 700; font-size: 0.88rem; cursor: pointer;
      transition: background 0.18s;
    }
    .city-search-form button:hover { background: var(--green-dark); }

    /* ── AREAS SECTION ── */
    .areas-section {
      padding: clamp(32px,5vw,56px) clamp(16px,5vw,60px);
      background: var(--white);
      border-bottom: 1px solid var(--border);
    }
    .areas-inner { max-width: 1280px; margin: 0 auto; }
    .areas-title {
      font-family: var(--font-d); font-size: 1.3rem; font-weight: 700;
      color: var(--ink); margin-bottom: 6px;
    }
    .areas-sub { font-size: 0.88rem; color: var(--muted); margin-bottom: 22px; }

    .areas-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(175px, 1fr));
      gap: 10px;
    }
    .area-chip {
      display: flex; align-items: center; justify-content: space-between;
      padding: 12px 14px;
      background: var(--bg); border: 1.5px solid var(--border);
      border-radius: var(--radius-md); cursor: pointer;
      transition: all 0.18s; text-decoration: none; color: inherit;
    }
    .area-chip:hover {
      border-color: var(--green); background: var(--green-light);
      transform: translateY(-2px); box-shadow: var(--shadow-xs);
      color: inherit;
    }
    .area-chip-left { display: flex; flex-direction: column; gap: 2px; }
    .area-chip-name { font-size: 0.88rem; font-weight: 700; color: var(--ink); }
    .area-chip-count { font-size: 0.72rem; color: var(--green); font-weight: 600; }
    .area-chip-arrow { color: var(--muted-light); font-size: 0.75rem; transition: color 0.18s, transform 0.18s; }
    .area-chip:hover .area-chip-arrow { color: var(--green); transform: translateX(3px); }

    /* See more toggle */
    .areas-hidden { display: none; }
    .areas-hidden.open { display: contents; }
    .see-more-areas-btn {
      display: inline-flex; align-items: center; gap: 6px;
      margin-top: 14px; font-size: 0.84rem; font-weight: 700;
      color: var(--green); background: var(--green-light);
      border: 1.5px solid var(--green-border);
      padding: 8px 18px; border-radius: 50px; cursor: pointer;
      transition: all 0.18s;
    }
    .see-more-areas-btn:hover { background: var(--green); color: #fff; border-color: var(--green); }

    /* ── LISTINGS SECTION ── */
    .city-listings-section {
      padding: clamp(40px,6vw,64px) clamp(16px,5vw,60px);
      background: var(--bg);
    }
    .city-listings-inner { max-width: 1280px; margin: 0 auto; }
    .city-listings-header {
      display: flex; align-items: flex-end; justify-content: space-between;
      flex-wrap: wrap; gap: 12px; margin-bottom: 24px;
    }
    .city-listings-title {
      font-family: var(--font-d); font-size: 1.3rem; font-weight: 700; color: var(--ink);
    }
    .see-all-btn {
      display: inline-flex; align-items: center; gap: 5px;
      font-size: 0.84rem; font-weight: 700; color: var(--green);
      padding: 8px 18px; border-radius: 50px;
      border: 1.5px solid var(--green-border); background: var(--green-light);
      text-decoration: none; transition: all 0.18s;
    }
    .see-all-btn:hover { background: var(--green); color: #fff; border-color: var(--green); }

    /* ── WHY HOSTELNODE BAND ── */
    .why-band {
      background: var(--green); padding: 40px clamp(16px,5vw,60px);
      text-align: center;
    }
    .why-band h2 {
      font-family: var(--font-d); font-size: 1.4rem; font-weight: 700; color: #fff; margin-bottom: 24px;
    }
    .why-items {
      display: flex; justify-content: center; gap: 32px; flex-wrap: wrap;
    }
    .why-item { display: flex; align-items: center; gap: 8px; color: rgba(255,255,255,0.9); font-size: 0.9rem; font-weight: 600; }
    .why-item i { font-size: 1.1rem; color: #fff; }

    /* ── FAQ SECTION ── */
    .faq-section {
      padding: clamp(40px,6vw,64px) clamp(16px,5vw,60px);
      background: var(--white); border-top: 1px solid var(--border);
    }
    .faq-inner { max-width: 760px; margin: 0 auto; }
    .faq-section h2 {
      font-family: var(--font-d); font-size: 1.4rem; font-weight: 700; color: var(--ink); margin-bottom: 24px;
    }
    .faq-item {
      border: 1.5px solid var(--border); border-radius: var(--radius-md);
      margin-bottom: 10px; overflow: hidden;
    }
    .faq-q {
      width: 100%; text-align: left; background: var(--bg);
      padding: 14px 18px; font-size: 0.93rem; font-weight: 600; color: var(--ink);
      border: none; cursor: pointer; display: flex; align-items: center; justify-content: space-between;
      gap: 10px; transition: background 0.18s;
    }
    .faq-q:hover, .faq-item.open .faq-q { background: var(--green-light); color: var(--green-dark); }
    .faq-q i { transition: transform 0.25s; color: var(--green); flex-shrink: 0; }
    .faq-item.open .faq-q i { transform: rotate(180deg); }
    .faq-a {
      max-height: 0; overflow: hidden; transition: max-height 0.3s ease;
      font-size: 0.88rem; color: var(--muted); line-height: 1.75;
    }
    .faq-item.open .faq-a { max-height: 400px; }
    .faq-a-inner { padding: 14px 18px; }

    /* ── NO LISTINGS ── */
    .no-listings { text-align: center; padding: 60px 20px; color: var(--muted); }
    .no-listings i { font-size: 2.5rem; margin-bottom: 14px; color: var(--muted-light); display: block; }

    @media (max-width: 600px) {
      .areas-grid { grid-template-columns: 1fr 1fr; }
      .city-stats-row { gap: 16px; }
      .why-items { gap: 16px; }
    }
  </style>
</head>
<body>

<%- include("../includes/navbar-user.ejs") %>

<!-- ═══════════════════════════════════════════
     CITY HERO
════════════════════════════════════════════ -->
<section class="city-hero">
  <div class="city-hero-inner">

    <!-- Breadcrumb -->
    <div class="city-breadcrumb">
      <a href="/">Home</a>
      <i class="fa-solid fa-chevron-right"></i>
      <a href="/findHostels">Find Hostel</a>
      <i class="fa-solid fa-chevron-right"></i>
      <span>PG in <%= cityData.name %></span>
    </div>

    <span class="city-emoji"><%= cityData.emoji %></span>

    <h1>
      Best PG & Hostel in<br>
      <span><%= cityData.name %></span>
    </h1>

    <p class="city-hero-sub">
      <%= totalListings %>+ verified PGs, hostels & flat sharing options in <%= cityData.name %>.
      Compare prices, amenities and connect directly with owners. Zero brokerage.
    </p>

    <!-- Quick search within city -->
    <div class="city-search-wrap">
      <form class="city-search-form" action="/findHostels/results" method="GET">
        <i class="fa-solid fa-magnifying-glass" style="color:var(--green);flex-shrink:0;"></i>
        <input type="text" name="college"
          placeholder="Search area in <%= cityData.name %>… (e.g. <%= cityData.areas[0].name %>)"
          value="">
        <input type="hidden" name="city" value="<%= cityData.name %>">
        <button type="submit"><i class="fa-solid fa-magnifying-glass"></i> Search</button>
      </form>
    </div>

    <!-- Stats strip -->
    <div class="city-stats-row">
      <div class="city-stat">
        <div class="city-stat-num"><%= totalListings %><span>+</span></div>
        <div class="city-stat-label">Verified listings</div>
      </div>
      <div class="city-stat">
        <div class="city-stat-num"><%= cityData.areas.length %><span>+</span></div>
        <div class="city-stat-label">Areas covered</div>
      </div>
      <div class="city-stat">
        <div class="city-stat-num">₹2k<span>–12k</span></div>
        <div class="city-stat-label">Price range</div>
      </div>
      <div class="city-stat">
        <div class="city-stat-num">0<span> fee</span></div>
        <div class="city-stat-label">Zero brokerage</div>
      </div>
    </div>

  </div>
</section>

<!-- ═══════════════════════════════════════════
     AREAS / SUB-LOCALITIES
════════════════════════════════════════════ -->
<section class="areas-section">
  <div class="areas-inner">
    <h2 class="areas-title">Search PG in <%= cityData.name %> by Area</h2>
    <p class="areas-sub">Select your preferred locality to see PGs & hostels nearby</p>

    <div class="areas-grid" id="areasGrid">
      <% areaCounts.slice(0, 10).forEach(function(area) { %>
        <a href="/city/<%= cityData.slug %>/<%= area.slug %>" class="area-chip"
           onclick="trackAreaClick('<%= area.name %>', '<%= cityData.name %>')">
          <div class="area-chip-left">
            <span class="area-chip-name"><%= area.name %></span>
            <span class="area-chip-count">
              <% if (area.count > 0) { %>
                <%= area.count %> listing<%= area.count !== 1 ? 's' : '' %>
              <% } else { %>
                Coming soon
              <% } %>
            </span>
          </div>
          <i class="fa-solid fa-arrow-right area-chip-arrow"></i>
        </a>
      <% }) %>

      <!-- Hidden areas shown on "See more" -->
      <% if (areaCounts.length > 10) { %>
        <div class="areas-hidden" id="areasHidden">
          <% areaCounts.slice(10).forEach(function(area) { %>
            <a href="/city/<%= cityData.slug %>/<%= area.slug %>" class="area-chip"
               onclick="trackAreaClick('<%= area.name %>', '<%= cityData.name %>')">
              <div class="area-chip-left">
                <span class="area-chip-name"><%= area.name %></span>
                <span class="area-chip-count">
                  <% if (area.count > 0) { %>
                    <%= area.count %> listing<%= area.count !== 1 ? 's' : '' %>
                  <% } else { %>
                    Coming soon
                  <% } %>
                </span>
              </div>
              <i class="fa-solid fa-arrow-right area-chip-arrow"></i>
            </a>
          <% }) %>
        </div>
      <% } %>
    </div>

    <% if (areaCounts.length > 10) { %>
      <button class="see-more-areas-btn" id="seeMoreAreasBtn" onclick="toggleAreas()">
        <i class="fa-solid fa-chevron-down" id="areasChevron"></i>
        See all <%= areaCounts.length %> areas
      </button>
    <% } %>
  </div>
</section>

<!-- ═══════════════════════════════════════════
     FEATURED LISTINGS
════════════════════════════════════════════ -->
<section class="city-listings-section">
  <div class="city-listings-inner">
    <div class="city-listings-header">
      <div>
        <h2 class="city-listings-title">
          Top PG & Hostels in <span style="color:var(--green)"><%= cityData.name %></span>
        </h2>
        <p style="font-size:0.82rem;color:var(--muted);margin-top:4px;">
          Showing <%= listings.length %> featured listings · <a href="/findHostels/results?college=<%= encodeURIComponent(cityData.name) %>" style="color:var(--green);font-weight:600;">View all</a>
        </p>
      </div>
      <a href="/findHostels/results?college=<%= encodeURIComponent(cityData.name) %>" class="see-all-btn">
        See all listings <i class="fa-solid fa-arrow-right"></i>
      </a>
    </div>

    <% if (listings && listings.length > 0) { %>
      <div class="cards-grid">
        <% listings.forEach(function(l) { %>
          <div class="hostel-card" data-id="<%= l._id %>" onclick="window.location.href='/hostel/<%= l.slug %>'">
            <div class="card-img-wrap">
              <div class="card-slider">
                <% if (l.images && l.images.length > 0) { %>
                  <% l.images.forEach(function(img, i) { %>
                    <img src="/listing-images/<%= img %>" class="slide <%= i === 0 ? 'active' : '' %>" alt="<%= l.title %>">
                  <% }) %>
                <% } else { %>
                  <img src="/images/placeholder.jpg" class="slide active" alt="<%= l.title %>">
                <% } %>
              </div>
              <button class="slide-btn prev" onclick="slideImg(event,this,-1)"><i class="fa-solid fa-chevron-left"></i></button>
              <button class="slide-btn next" onclick="slideImg(event,this,1)"><i class="fa-solid fa-chevron-right"></i></button>
              <span class="card-badge badge-<%= (l.gender || '').toLowerCase() %>"><%= l.gender %></span>
              <% if (l.isVerified) { %>
                <span class="card-badge" style="left:72px;top:12px;background:#fff;color:#09b850;margin-left:4px;">
                  <i class="fa-solid fa-shield-halved"></i> Verified
                </span>
              <% } %>
              <div class="card-wishlist" onclick="event.stopPropagation()">
                <i class="fa-regular fa-heart"></i>
              </div>
              <div class="card-dist">
                <i class="fa-solid fa-location-dot"></i>
                <%= l.location?.nearCollege || l.location?.city %>
              </div>
            </div>
            <div class="card-body">
              <div class="card-top">
                <div>
                  <div class="card-name"><%= l.title %></div>
                  <div class="card-best-value"><i class="fa-solid fa-bolt"></i> <%= l.propertyType %></div>
                </div>
                <div class="card-rating">
                  <i class="fa-solid fa-star"></i> <%= l.rating || 4.2 %>
                  <span style="font-size:0.7rem;color:var(--muted-light)">(<%= l.reviewCount || 0 %>)</span>
                </div>
              </div>
              <div class="card-loc">
                <i class="fa-solid fa-location-dot"></i>
                <%= l.location?.address %>, <%= l.location?.city %>
              </div>
              <div class="card-amenities">
                <% if (l.amenities) { %>
                  <% l.amenities.slice(0,4).forEach(function(a) { %>
                    <span class="amenity-tag"><%= a %></span>
                  <% }) %>
                <% } %>
              </div>
              <div class="card-footer-row">
                <div class="card-price">
                  <div class="price-val">₹<%= l.startingPrice %> <span>/mo</span></div>
                </div>
                <button class="btn-view" onclick="event.stopPropagation();window.location.href='/hostel/<%= l.slug %>'">
                  View <i class="fa-solid fa-arrow-right"></i>
                </button>
              </div>
              <div class="card-contact-row">
                <button class="btn-wa" onclick="event.stopPropagation();waHostel('<%= l.contact?.whatsapp %>')">
                  <i class="fa-brands fa-whatsapp"></i> WhatsApp
                </button>
                <button class="btn-call" onclick="event.stopPropagation();callHostel('<%= l.contact?.phone %>')">
                  <i class="fa-solid fa-phone"></i> Call
                </button>
              </div>
            </div>
          </div>
        <% }) %>
      </div>

      <div style="text-align:center;margin-top:32px;">
        <a href="/findHostels/results?college=<%= encodeURIComponent(cityData.name) %>" class="btn-load-more" style="display:inline-flex;align-items:center;gap:8px;text-decoration:none;">
          <i class="fa-solid fa-arrow-right"></i> View all PGs in <%= cityData.name %>
        </a>
      </div>

    <% } else { %>
      <div class="no-listings">
        <i class="fa-solid fa-building"></i>
        <h3 style="font-size:1.1rem;font-weight:700;color:var(--ink);margin-bottom:8px;">Listings coming soon!</h3>
        <p>We're adding hostels in <%= cityData.name %>. <a href="/signup" style="color:var(--green);font-weight:600;">List yours for free →</a></p>
      </div>
    <% } %>
  </div>
</section>

<!-- ═══════════════════════════════════════════
     WHY HOSTELNODE BAND
════════════════════════════════════════════ -->
<div class="why-band">
  <h2>Why students choose HostelNode in <%= cityData.name %></h2>
  <div class="why-items">
    <div class="why-item"><i class="fa-solid fa-shield-halved"></i> 100% Verified listings</div>
    <div class="why-item"><i class="fa-solid fa-hand-holding-dollar"></i> Zero broker fees</div>
    <div class="why-item"><i class="fa-solid fa-phone"></i> Direct owner contact</div>
    <div class="why-item"><i class="fa-solid fa-star"></i> Real student reviews</div>
    <div class="why-item"><i class="fa-solid fa-location-crosshairs"></i> Location-based search</div>
  </div>
</div>

<!-- ═══════════════════════════════════════════
     FAQ (SEO boost)
════════════════════════════════════════════ -->
<section class="faq-section">
  <div class="faq-inner">
    <h2>Frequently Asked Questions — PG in <%= cityData.name %></h2>

    <%
      const faqs = [
        {
          q: `What is the average PG rent in ${cityData.name}?`,
          a: `PG rent in ${cityData.name} typically ranges from ₹3,000 to ₹12,000 per month depending on the area, room type (single/double/triple sharing), and amenities included like meals, WiFi, and AC. Areas near tech parks and colleges tend to be on the higher side.`
        },
        {
          q: `How do I find a verified PG in ${cityData.name} on HostelNode?`,
          a: `Simply search "${cityData.name}" on HostelNode, filter by your preferred area, budget, and gender. All listings marked with a green shield are verified by our team. You can contact owners directly via call or WhatsApp — no brokerage.`
        },
        {
          q: `Which areas in ${cityData.name} have the most PG options?`,
          a: `${cityData.areas.slice(0, 5).map(a => a.name).join(", ")} are among the most popular areas for PGs in ${cityData.name}, especially for students and working professionals.`
        },
        {
          q: `Are there girls-only PGs in ${cityData.name}?`,
          a: `Yes! HostelNode lists many girls-only PGs and hostels in ${cityData.name} with safety features like CCTV, security guards, and warden facilities. Use the gender filter to find them.`
        },
        {
          q: `Is HostelNode free to use for students?`,
          a: `Absolutely. HostelNode is 100% free for students. No registration fee, no brokerage, no hidden charges. Connect directly with verified PG owners in ${cityData.name}.`
        },
      ];
    %>

    <% faqs.forEach(function(faq, i) { %>
      <div class="faq-item" id="faq<%= i %>">
        <button class="faq-q" onclick="toggleFaq(<%= i %>)">
          <%= faq.q %>
          <i class="fa-solid fa-chevron-down"></i>
        </button>
        <div class="faq-a">
          <div class="faq-a-inner"><%= faq.a %></div>
        </div>
      </div>
    <% }) %>
  </div>
</section>

<!-- ═══════════════════════════════════════════
     FOOTER
════════════════════════════════════════════ -->
 

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<script>
  /* ── Areas toggle ── */
  function toggleAreas() {
    const hidden  = document.getElementById('areasHidden');
    const btn     = document.getElementById('seeMoreAreasBtn');
    const chevron = document.getElementById('areasChevron');
    const isOpen  = hidden.classList.toggle('open');
    chevron.style.transform = isOpen ? 'rotate(180deg)' : 'rotate(0)';
    btn.innerHTML = isOpen
      ? '<i class="fa-solid fa-chevron-up" id="areasChevron" style="transform:rotate(180deg)"></i> Show less'
      : '<i class="fa-solid fa-chevron-down" id="areasChevron"></i> See all areas';
  }

  /* ── FAQ accordion ── */
  function toggleFaq(i) {
    const item = document.getElementById('faq' + i);
    item.classList.toggle('open');
  }

  /* ── Image slider ── */
  function slideImg(e, btn, dir) {
    e.stopPropagation();
    const wrap   = btn.closest('.card-img-wrap');
    const slides = wrap.querySelectorAll('.slide');
    let idx = [...slides].findIndex(s => s.classList.contains('active'));
    slides[idx].classList.remove('active');
    slides[(idx + dir + slides.length) % slides.length].classList.add('active');
  }

  /* ── Contact helpers ── */
  function callHostel(phone) { if (phone) window.location.href = 'tel:' + phone; }
  function waHostel(phone) {
    const n = (phone || '').replace(/[^0-9]/g, '');
    if (!n) return;
    window.open('https://wa.me/' + n + '?text=' + encodeURIComponent('Hi! I found your PG on HostelNode and am interested. Can you share details?'), '_blank');
  }

  /* ── Track area click (GA4) ── */
  function trackAreaClick(area, city) {
    if (typeof gtag !== 'undefined') {
      gtag('event', 'area_click', { event_category: 'city_page', event_label: area + ' - ' + city });
    }
  }

  /* ── Navbar scroll ── */
  window.addEventListener('scroll', () => {
    document.getElementById('hnNav')?.classList.toggle('scrolled', scrollY > 10);
  }, { passive: true });
</script>



<% if (!student) { %>

<!-- ══ ENQUIRY/SIGNUP MODAL (same as hostel-view) ══ -->
<style>
.enq-overlay{position:fixed;inset:0;background:rgba(0,0,0,0.55);backdrop-filter:blur(3px);z-index:99999;display:flex;align-items:center;justify-content:center;padding:16px;opacity:0;pointer-events:none;transition:opacity .22s;}
.enq-overlay.open{opacity:1;pointer-events:all;}
.enq-box{background:#fff;border-radius:22px;width:100%;max-width:460px;max-height:92vh;overflow-y:auto;position:relative;box-shadow:0 20px 60px rgba(0,0,0,.22);transform:translateY(20px);transition:transform .25s;scrollbar-width:thin;}
.enq-overlay.open .enq-box{transform:translateY(0);}
.enq-close{position:absolute;top:14px;right:14px;width:32px;height:32px;border-radius:50%;border:none;background:rgba(0,0,0,.08);color:#555;font-size:15px;cursor:pointer;z-index:10;display:flex;align-items:center;justify-content:center;}
.enq-header{padding:28px 24px 22px;text-align:center;}
.enq-header-auth{background:linear-gradient(135deg,#0a1628,#1a3a5c);}
.enq-header h3{font-family:'Instrument Sans',sans-serif;font-size:20px;font-weight:700;color:#fff;margin:0 0 4px;}
.enq-header p{color:rgba(255,255,255,.75);font-size:13px;margin:0;}
.enq-header-icon{font-size:32px;margin-bottom:10px;}
.enq-body{padding:22px 24px 24px;}
.enq-alert{display:none;padding:10px 14px;border-radius:10px;font-size:13px;margin-bottom:14px;}
.enq-alert.show{display:block;}
.enq-alert.error{background:#fff5f5;color:#c53030;border:1px solid #feb2b2;}
.enq-alert.success{background:#e8faf0;color:#078a3c;border:1px solid #9ae6b4;}
.enq-field{margin-bottom:14px;}
.enq-field label{display:block;font-size:12px;font-weight:600;color:#3d4f63;margin-bottom:6px;text-transform:uppercase;letter-spacing:.4px;}
.enq-field input[type="text"],.enq-field input[type="tel"]{width:100%;padding:11px 13px;border:1.5px solid #d6e4d9;border-radius:12px;font-size:14.5px;font-family:inherit;color:#0a1628;background:#fff;outline:none;box-sizing:border-box;}
.enq-field input:focus{border-color:#09B850;box-shadow:0 0 0 3px rgba(9,184,80,.12);}
.enq-otp-row{display:flex;gap:10px;}
.enq-btn-otp{padding:10px 14px;background:#e8faf0;color:#078a3c;border:1.5px solid #09B850;border-radius:12px;font-size:13px;font-weight:600;cursor:pointer;white-space:nowrap;font-family:inherit;flex-shrink:0;}
.enq-btn-otp:hover{background:#09B850;color:white;}
.enq-btn-otp:disabled{opacity:.5;cursor:not-allowed;}
.enq-countdown{font-size:12px;color:#64748b;margin-top:5px;}
.enq-countdown span{color:#09B850;font-weight:600;}
.enq-verified-badge{display:inline-flex;align-items:center;gap:5px;background:#e8faf0;color:#078a3c;font-size:12px;font-weight:600;padding:4px 12px;border-radius:20px;}
.enq-row-2{display:grid;grid-template-columns:1fr 1fr;gap:12px;}
.enq-btn-primary{width:100%;padding:13px;background:#09B850;color:white;border:none;border-radius:13px;font-size:15px;font-weight:700;font-family:'Instrument Sans',sans-serif;cursor:pointer;margin-top:6px;display:flex;align-items:center;justify-content:center;gap:8px;}
.enq-btn-primary:hover{background:#078a3c;}
.enq-btn-primary:disabled{opacity:.6;cursor:not-allowed;}
.enq-login-note{text-align:center;font-size:13px;color:#64748b;margin-top:14px;}
.enq-login-note a{color:#09B850;font-weight:600;text-decoration:none;}
.enq-success{text-align:center;padding:48px 24px;}
.enq-success-icon{font-size:56px;margin-bottom:16px;}
.enq-success h3{font-family:'Instrument Sans',sans-serif;font-size:22px;font-weight:700;color:#0a1628;margin-bottom:8px;}
.enq-success p{color:#64748b;font-size:15px;max-width:300px;margin:0 auto 24px;}
@media(max-width:480px){.enq-body{padding:18px 18px 22px;}.enq-row-2{grid-template-columns:1fr;}}
</style>

<div class="enq-overlay" id="enqOverlay">
  <div class="enq-box" onclick="event.stopPropagation()">
    <button class="enq-close" onclick="closeNudgeModal()">
      <i class="fa-solid fa-xmark"></i>
    </button>

    <!-- AUTH STEP -->
    <div id="nudgeStepAuth">
      <div class="enq-header enq-header-auth">
        <div class="enq-header-icon">🔑</div>
        <h3>Quick login to explore</h3>
        <p>30 seconds — save hostels, send enquiries, get alerts</p>
      </div>
      <div class="enq-body">
        <div class="enq-alert" id="nudgeAlert"></div>

        <!-- Signup Panel -->
        <div id="nudgePanel1">
          <div class="enq-row-2">
            <div class="enq-field">
              <label>First Name *</label>
              <input type="text" id="nudgeFirst" placeholder="Rahul">
            </div>
            <div class="enq-field">
              <label>Last Name *</label>
              <input type="text" id="nudgeLast" placeholder="Sharma">
            </div>
          </div>
          <div class="enq-field">
            <label>WhatsApp Mobile *</label>
            <div class="enq-otp-row">
              <input type="tel" id="nudgePhone" placeholder="10-digit number" maxlength="10" inputmode="numeric" style="flex:1">
              <button class="enq-btn-otp" id="nudgeSendBtn" onclick="nudgeSendOtp()">Send OTP</button>
            </div>
            <div class="enq-countdown" id="nudgeCountdown" style="display:none">
              Resend in <span id="nudgeTimer">60</span>s
            </div>
          </div>
          <div class="enq-field" id="nudgeOtpField" style="display:none">
            <label>OTP (WhatsApp) *</label>
            <div class="enq-otp-row">
              <input type="text" id="nudgeOtp" placeholder="————" maxlength="4" inputmode="numeric"
                style="flex:1;letter-spacing:5px;font-size:20px;font-weight:700;text-align:center">
              <button class="enq-btn-otp" id="nudgeVerifyBtn" onclick="nudgeVerifyOtp()">Verify</button>
            </div>
            <div id="nudgeVerifiedBadge" style="display:none;margin-top:6px">
              <span class="enq-verified-badge">✅ Verified</span>
            </div>
          </div>
          <button class="enq-btn-primary" id="nudgeSignupBtn" onclick="nudgeSignup()" disabled>
            🚀 Create Account & Explore →
          </button>
          <p class="enq-login-note">Already have an account? <a href="#" onclick="nudgeSwitchLogin()">Login instead</a></p>
        </div>

        <!-- Login Panel -->
        <div id="nudgePanel2" style="display:none">
          <div class="enq-field">
            <label>Your Mobile *</label>
            <div class="enq-otp-row">
              <input type="tel" id="nudgeLoginPhone" placeholder="10-digit number" maxlength="10" inputmode="numeric" style="flex:1">
              <button class="enq-btn-otp" id="nudgeLoginSendBtn" onclick="nudgeLoginSendOtp()">Send OTP</button>
            </div>
            <div class="enq-countdown" id="nudgeLoginCountdown" style="display:none">
              Resend in <span id="nudgeLoginTimer">60</span>s
            </div>
          </div>
          <div class="enq-field" id="nudgeLoginOtpField" style="display:none">
            <label>OTP (WhatsApp) *</label>
            <input type="text" id="nudgeLoginOtp" placeholder="————" maxlength="4" inputmode="numeric"
              style="letter-spacing:5px;font-size:20px;font-weight:700;text-align:center;width:100%;padding:11px 13px;border:1.5px solid #d6e4d9;border-radius:12px;outline:none">
            <div id="nudgeLoginVerifiedBadge" style="display:none;margin-top:6px">
              <span class="enq-verified-badge">✅ Verified</span>
            </div>
          </div>
          <button class="enq-btn-primary" id="nudgeLoginBtn" onclick="nudgeLogin()" disabled>
            Login & Continue →
          </button>
          <p class="enq-login-note">New here? <a href="#" onclick="nudgeSwitchSignup()">Sign up instead</a></p>
        </div>
      </div>
    </div>

    <!-- SUCCESS STEP -->
    <div id="nudgeStepSuccess" style="display:none">
      <div class="enq-success">
        <div class="enq-success-icon">🎉</div>
        <h3>Welcome to HostelNode!</h3>
        <p>You're all set. Explore verified hostels near your college.</p>
        <button class="enq-btn-primary" onclick="closeNudgeModal()">Start Exploring →</button>
      </div>
    </div>

  </div>
</div>

<script>
(function () {
  if (sessionStorage.getItem('hn_nudge_shown')) return;

  var timer = null;
  function startTimer() {
    if (timer) return;
    timer =  setTimeout(openNudgeModal, 30000);
  }

  ['mousemove','scroll','click','touchstart','keydown'].forEach(function(evt) {
    window.addEventListener(evt, function() { startTimer(); }, { once: true });
  });
})();

function openNudgeModal() {
  document.getElementById('enqOverlay').classList.add('open');
  document.body.style.overflow = 'hidden';
  sessionStorage.setItem('hn_nudge_shown', 'true');
}

function closeNudgeModal() {
  document.getElementById('enqOverlay').classList.remove('open');
  document.body.style.overflow = '';
}

// Close on overlay background click
document.getElementById('enqOverlay').addEventListener('click', function(e) {
  if (e.target === this) closeNudgeModal();
});

function showNudgeAlert(msg, type) {
  var el = document.getElementById('nudgeAlert');
  el.textContent = msg;
  el.className = 'enq-alert show ' + (type || 'error');
  setTimeout(function(){ el.classList.remove('show'); }, 4000);
}

function nudgeSwitchLogin() {
  document.getElementById('nudgePanel1').style.display = 'none';
  document.getElementById('nudgePanel2').style.display = 'block';
}
function nudgeSwitchSignup() {
  document.getElementById('nudgePanel1').style.display = 'block';
  document.getElementById('nudgePanel2').style.display = 'none';
}

function startNudgeCountdown(timerId, countdownId, btnId) {
  var s = 60;
  var cd = document.getElementById(countdownId);
  var t  = document.getElementById(timerId);
  var b  = document.getElementById(btnId);
  cd.style.display = 'block';
  b.disabled = true;
  var iv = setInterval(function() {
    t.textContent = --s;
    if (s <= 0) {
      clearInterval(iv);
      cd.style.display = 'none';
      b.disabled = false;
      b.textContent = 'Resend';
    }
  }, 1000);
}

var nudgeOtpVerified = false;
var nudgeLoginOtpVerified = false;

/* ── SIGNUP OTP ── */
async function nudgeSendOtp() {
  var phone = document.getElementById('nudgePhone').value.trim();
  if (!/^[6-9]\d{9}$/.test(phone)) return showNudgeAlert('Enter a valid 10-digit number');
  var btn = document.getElementById('nudgeSendBtn');
  btn.disabled = true; btn.textContent = 'Sending…';
  var res = await fetch('/student/send-otp', {
    method:'POST', headers:{'Content-Type':'application/json'},
    body: JSON.stringify({ phone })
  }).catch(function(){ return null; });
  if (!res) { btn.disabled=false; btn.textContent='Send OTP'; return showNudgeAlert('Network error'); }
  var data = await res.json();
  if (!data.success) {
    btn.disabled=false; btn.textContent='Send OTP';
    if (data.error?.toLowerCase().includes('registered')) {
      showNudgeAlert('Already registered — switching to login', 'success');
      return setTimeout(nudgeSwitchLogin, 1200);
    }
    return showNudgeAlert(data.error || 'Failed to send OTP');
  }
  document.getElementById('nudgeOtpField').style.display = 'block';
  showNudgeAlert('OTP sent to WhatsApp ✅', 'success');
  startNudgeCountdown('nudgeTimer', 'nudgeCountdown', 'nudgeSendBtn');
}

async function nudgeVerifyOtp() {
  var phone = document.getElementById('nudgePhone').value.trim();
  var otp   = document.getElementById('nudgeOtp').value.trim();
  if (otp.length !== 4) return;
  var res = await fetch('/student/verify-otp', {
    method:'POST', headers:{'Content-Type':'application/json'},
    body: JSON.stringify({ phone, otp })
  }).catch(function(){ return null; });
  if (!res) return showNudgeAlert('Network error');
  var data = await res.json();
  if (!data.success) return showNudgeAlert(data.error || 'Wrong OTP');
  nudgeOtpVerified = true;
  document.getElementById('nudgeVerifiedBadge').style.display = 'block';
  document.getElementById('nudgeOtp').disabled = true;
  document.getElementById('nudgeVerifyBtn').disabled = true;
  document.getElementById('nudgeSignupBtn').disabled = false;
  showNudgeAlert('Verified! Click Create Account ✅', 'success');
}

async function nudgeSignup() {
  if (!nudgeOtpVerified) return showNudgeAlert('Verify your mobile first');
  var firstName = document.getElementById('nudgeFirst').value.trim();
  var lastName  = document.getElementById('nudgeLast').value.trim();
  var phone     = document.getElementById('nudgePhone').value.trim();
  if (firstName.length < 2) return showNudgeAlert('Enter your first name');
  if (lastName.length  < 2) return showNudgeAlert('Enter your last name');
  var btn = document.getElementById('nudgeSignupBtn');
  btn.disabled = true; btn.textContent = 'Creating account…';
  var res = await fetch('/student/signup', {
    method:'POST', headers:{'Content-Type':'application/json'},
    body: JSON.stringify({ firstName, lastName, phone })
  }).catch(function(){ return null; });
  if (!res) { btn.disabled=false; btn.textContent='Create Account →'; return showNudgeAlert('Network error'); }
  var data = await res.json();
  if (!data.success) {
    btn.disabled=false; btn.textContent='Create Account →';
    return showNudgeAlert(data.error || 'Signup failed');
  }
  document.getElementById('nudgeStepAuth').style.display = 'none';
  document.getElementById('nudgeStepSuccess').style.display = 'block';
}

/* ── LOGIN OTP ── */
async function nudgeLoginSendOtp() {
  var phone = document.getElementById('nudgeLoginPhone').value.trim();
  if (!/^[6-9]\d{9}$/.test(phone)) return showNudgeAlert('Enter a valid 10-digit number');
  var btn = document.getElementById('nudgeLoginSendBtn');
  btn.disabled = true; btn.textContent = 'Sending…';
  var res = await fetch('/student/send-otp', {
    method:'POST', headers:{'Content-Type':'application/json'},
    body: JSON.stringify({ phone })
  }).catch(function(){ return null; });
  if (!res) { btn.disabled=false; btn.textContent='Send OTP'; return showNudgeAlert('Network error'); }
  var data = await res.json();
  if (!data.success) {
    btn.disabled=false; btn.textContent='Send OTP';
    if (data.error?.toLowerCase().includes('no account')) {
      showNudgeAlert('No account found — switching to signup', 'success');
      return setTimeout(nudgeSwitchSignup, 1200);
    }
    return showNudgeAlert(data.error || 'Failed to send OTP');
  }
  document.getElementById('nudgeLoginOtpField').style.display = 'block';
  showNudgeAlert('OTP sent ✅', 'success');
  startNudgeCountdown('nudgeLoginTimer', 'nudgeLoginCountdown', 'nudgeLoginSendBtn');
  document.getElementById('nudgeLoginOtp').addEventListener('input', async function() {
    if (this.value.length !== 4 || nudgeLoginOtpVerified) return;
    var r = await fetch('/student/verify-otp', {
      method:'POST', headers:{'Content-Type':'application/json'},
      body: JSON.stringify({ phone, otp: this.value })
    }).catch(function(){ return null; });
    if (!r) return;
    var d = await r.json();
    if (d.success) {
      nudgeLoginOtpVerified = true;
      this.disabled = true;
      document.getElementById('nudgeLoginVerifiedBadge').style.display = 'block';
      document.getElementById('nudgeLoginBtn').disabled = false;
      showNudgeAlert('Verified! Click Login ✅', 'success');
    } else {
      showNudgeAlert(d.error || 'Wrong OTP');
    }
  });
}

async function nudgeLogin() {
  if (!nudgeLoginOtpVerified) return showNudgeAlert('Verify OTP first');
  var phone = document.getElementById('nudgeLoginPhone').value.trim();
  var btn   = document.getElementById('nudgeLoginBtn');
  btn.disabled = true; btn.textContent = 'Logging in…';
  var res = await fetch('/student/login', {
    method:'POST', headers:{'Content-Type':'application/json'},
    body: JSON.stringify({ phone })
  }).catch(function(){ return null; });
  if (!res) { btn.disabled=false; btn.textContent='Login →'; return showNudgeAlert('Network error'); }
  var data = await res.json();
  if (!data.success) {
    btn.disabled=false; btn.textContent='Login →';
    return showNudgeAlert(data.error || 'Login failed');
  }
  document.getElementById('nudgeStepAuth').style.display = 'none';
  document.getElementById('nudgeStepSuccess').style.display = 'block';
}
</script>

<% } %>


<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
<script>
  /* ═════ PROFILE DROPDOWN ════ */
  function snToggle(e) {
    e.stopPropagation();
    const pill = document.getElementById('snPill');
    if (pill) pill.classList.toggle('open');
  }

  document.addEventListener('click', function () {
    const pill = document.getElementById('snPill');
    if (pill) pill.classList.remove('open');
  });

  /* ── Areas toggle ── */
  function toggleAreas() {
    // ... rest of your code
  }
  
  // ... other functions
</script>

</body>
</html>
