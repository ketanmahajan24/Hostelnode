// ============================================================
//  utils/flatmateMilestones.js — HostelNode Flatmate (Phase 10)
// ============================================================
// View-count thresholds that trigger a one-time "your listing hit N
// views" notification to the owner. Kept as a single exported array
// so the list only needs changing in one place.
const VIEW_MILESTONES = [10, 50, 100, 250, 500, 1000];

module.exports = { VIEW_MILESTONES };
